"""Phase 4.1 — selector readiness helpers (no selector logic yet).

Phase 5 will introduce a daily-selector module here. Until then, this
package contains a PURE `compute_readiness(...)` function that the
scanner uses to populate audit fields on each candidate row.
"""
